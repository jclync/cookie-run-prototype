{
  "devDependencies": {
    "@web/dev-server": "^0.1.35",
    "gh-pages": "^5.0.0",
    "vite": "^4.1.4"
  },
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/cookie-run-prototype/ && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^9.17.2",
    "firebaseui": "^6.0.2",
    "lit-html": "^2.6.1"
  }
}
